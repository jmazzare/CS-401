<?php

class User {
    function __construct($email) {
        $this->email = $email;
    }
}

function rolling_averages($users, $activity) {
    // TODO: calculate the rolling averages as described in the writeup
    return [];
}
